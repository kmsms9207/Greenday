name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  # ✅ 올바른 위치: flutter와 같은 레벨 (두 칸 들여쓰기)
  dio: ^5.0.0 
  image_picker: ^1.2.0
  flutter_secure_storage: ^9.2.4
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  http_parser: 4.1.2
  intl: ^0.20.2
  cached_network_image: ^3.4.1
  firebase_core: ^4.2.0
  firebase_messaging: ^16.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/terms.txt
    - assets/privacy.txt
    - assets/marketing.txt